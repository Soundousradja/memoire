
    function handleLogin() {
        alert("Redirection vers la page de connexion...");
    }
    
    function navigateTo(url) {
        console.log("Redirection vers : " + url); // Vérifie l'URL dans la console
        window.location.href = url;
    }
    